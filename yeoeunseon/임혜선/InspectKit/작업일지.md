## 2026-06-24

- InspectKit 프로젝트 Git 초기화 및 첫 커밋 (`Initial commit`)
- `.gitignore` 작성 (C++/Visual Studio 빌드 아티팩트, `.vs/`, `*.user` 등 제외)
- GitHub 원격 레포지토리 연결 (`https://github.com/1754269/InspectKit.git`)
- `main` 브랜치 최초 푸시 완료
- OpenCV 설치 완료

### 내일 할 일
- 콘솔 앱에서 이미지 읽어서 띄우기

## 2026-06-25

- 콘솔 앱에서 OpenCV로 이미지 로드/표시 테스트 완료 (커밋: `OpenCV 이미지 로드 테스트 추가 및 gitignore 추가`)
- 옵시디언 `development plan.md` 검토 → M1(엔진 코어)부터 구현하기로 결정
- **설계 결정 ①**: M1부터 `extern "C"` C API로 엔진-콘솔 연결 (C++ 직결 대신). M3 재작업 방지 + DLL 경계(OpenCV/STL 격리) 처음부터 확보
- **설계 결정 ②**: 원형 검출을 `HoughCircles` → **윤곽선(`findContours`)** 방식으로 변경
  - 이유: HoughCircles는 엣지 점을 숨겨 M2 sub-pixel로 못 이어짐. contour는 윤곽선 점 전체를 줘서 M1(지름)·M2(sub-pixel)·손상판정이 하나의 데이터로 연결됨
- `development plan.md` 업데이트: M1 검출 방식 contour로 변경, 근거 callout 추가, M2 연결 명시, 리스크 표 보강
- `M1 implementation plan.md` 신규 작성: M1 파이프라인(전처리→Otsu이진화→findContours→원형도 필터→minEnclosingCircle 지름), 파일 단위 작업(헤더→빌드→엔진→콘솔), Mermaid 다이어그램, 열린 결정(콘솔 이미지 디코딩 방식) 정리

### 내일 할 일
- M1 Step 1: `InspectEngine/include/InspectEngine.h` C API 헤더 작성 (OpenCV/STL 노출 없이)
- 구현 전 콘솔 이미지 디코딩 방식 확정 (엔진 `Insp_RunFile` 보조 API vs 콘솔 stb_image)
